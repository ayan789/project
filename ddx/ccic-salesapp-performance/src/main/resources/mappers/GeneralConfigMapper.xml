<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="com.ccic.salesapp.performance.repository.basedb.mapper.GeneralConfigMapper">

	<select id="getConfigValueNoCache" resultType="java.lang.String"
		parameterType="java.util.HashMap">
		select c.config_value from t_noncar_general_config c
		where c.valid_status='1'
		<if test="comCode!= null">
			and c.com_code=#{comCode}
		</if>
		<if test="riskCode!= null">
			and c.risk_code=#{riskCode}
		</if>
		and c.config_type= #{configType} limit 1
	</select>

</mapper>